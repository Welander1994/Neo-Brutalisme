{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --host",
    "json-server": "json-server --watch db.json --port 4000",
    "start": "concurrently \"npm run dev --host\" \"npm run json-server\"",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "concurrently": "^9.0.1",
    "json-server": "^1.0.0-beta.3",
    "nuxt": "^3.13.0",
    "vue": "latest",
    "vue-router": "latest"
  }
}
